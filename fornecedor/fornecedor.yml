spring:
  datasource:
    url: jdbc:mysql://localhost:3306/microservice_fornecedor?useTimezone=true&serverTimezone=UTC
    
    username: root
    password: root
    
    tomcat:
      test-while-idle: true
      
      validation-query: SELECT 1
      
      driver-class-name: com.mysql.cj.jdbc.Driver
      
  jpa:


    show-sql: true
    hibernate:
      ddl-auto: update
      
    properties:
      hibernate:
      dialect: org.hibernate.dialect.MySQL5InnoDBDialect
